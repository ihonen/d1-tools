#pragma once

// ----------------------------------------------------------------------------

void bzip2Decompress(
    void* dest,
    unsigned* destLen,
    const void* src,
    unsigned srcLen
);
